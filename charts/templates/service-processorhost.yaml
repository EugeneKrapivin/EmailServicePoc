apiVersion: v1
kind: Service
metadata:
  name: processorhost
  labels:
    {{- include "email-processor.labels" . | nindent 4 }}
spec:
  selector:
    app: processorhost
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
    - protocol: TCP
      port: 443
      targetPort: 443